import React from "react";
import { Button } from "@mantine/core";

function SaveButton() {
  return (
    <Button className="rounded-pill" size="md" color="pink" type="submit">
      ADD
    </Button>
  );
}

export default SaveButton;
